import { Queue } from "./Queue";
/** A queue that will accept unique values and handle each value one by one. */
export declare class UniqueQueue<T> extends Queue<T> {
    append(newValue: T): void;
}
//# sourceMappingURL=UniqueQueue.d.ts.map